{% extends 'vitrine/base.html.twig' %}

{% block title %}{{ filiere.nom }} - SchoolPrepar{% endblock %}

{% block body %}
<div class="hero-wrap" style="background-image: url('images/bg_2.jpg');" data-stellar-background-ratio="0.5">
    <div class="overlay"></div>
    <div class="container">
        <div class="row no-gutters slider-text align-items-center justify-content-center" data-scrollax-index="0">
            <div class="col-md-9 ftco-animate text-center">
                <h1 class="mb-3 bread">{{ filiere.nom }}</h1>
                <p class="breadcrumbs"><span class="mr-2"><a href="{{ path('app_home') }}">Accueil</a></span> <span><a href="{{ path('app_orientation') }}">Orientation</a></span> <span><a href="{{ path('app_filieres') }}">Filières</a></span> <span>{{ filiere.nom }}</span></p>
            </div>
        </div>
    </div>
</div>

<section class="ftco-section">
    <div class="container">
        <div class="row">
            <div class="col-md-8">
                <h2>{{ filiere.nom }}</h2>
                <p>{{ filiere.description }}</p>
            </div>
        </div>
    </div>
</section>
{% endblock %}
